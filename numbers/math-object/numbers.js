// JavaScript Numbers
//
//  The Math Object
//
//

var u = Math.random(); // will return a random number between 0 and 1 including the remainder

var v = Math.random() * 10; // will return a random number between 0 and 10 including the remainder

var w = Math.round(Math.random() * 10); // will round the random number between 0 and 10 up or down depending if .5 or not

var x = Math.round(2.3); // 2

var y = Math.round(2.7); // 3

var z = Math.round(2.5); // 3

var a = Math.floor(3.7); // floor will always round DOWN.

var b = Math.ceil(6.3); // ceil will always round UP.

var c = Math.pow(2, 5); // 2 to the power of 5. | 2*2=4*2=8*2=16*2=32

var d = Math.sqrt(4); // square root of 4

var e = Math.sqrt(5); // square root of 5

var f = Math.sqrt(2); // square root of 2

var g = Math.min(2, 4); //return the minimum

var h = Math.max(2, 4); //return the maximum

var i = Math.min(2, 4, 1); //return the minimum

var j = Math.abs(-1); // 1

var k = Math.PI;  // value of PI


