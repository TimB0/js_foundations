// JavaScript Arrays

var x = ['some', 'extra', 'words', 'here'];
//console.log(x.length);

var y = ['a string', 3, true, 'treehouse', function (a, b) {return a + b;}]

var z = ['a string', 3, ['a sub array', 2, 3, 4], 'treehouse', function (a, b) {return a + b;}]

var a = [];

// Alternative way to create an array is the Array Constructor
//  This is NOT recommended, but you may see it in other peoples code
// However it "could" be useful if you want to create an empty array with a known
// number objects

var b = new Array(50);