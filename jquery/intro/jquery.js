// Any valid css selector works with jquery

$("string");
$("#container");// an element with the id of container
$("nav a"); // all the links nested in a nav element
$("ul li a");
$(".error p");
$("div p:last-child");

// Write how you'd use jQuery to select an element with id of 'container'
$("#container");

// On the next line, use jQuery to select all list items (li) in an unordered list (ul) with the class of 'nav'?
$("ul.nav li");

